import{_ as s,C as d,o,c,j as t,a,b as l,w as n,E as r,a3 as p,a2 as h}from"./chunks/framework.CvZWOya4.js";const f=JSON.parse('{"title":"Project Modules","description":"","frontmatter":{},"headers":[],"relativePath":"implementation/modules.md","filePath":"implementation/modules.md","lastUpdated":1783956008000}'),u={name:"implementation/modules.md"};function k(m,e,g,C,F,y){const i=d("Mermaid");return o(),c("div",null,[e[1]||(e[1]=t("h1",{id:"project-modules",tabindex:"-1"},[a("Project Modules "),t("a",{class:"header-anchor",href:"#project-modules","aria-label":'Permalink to "Project Modules"'},"​")],-1)),e[2]||(e[2]=t("p",null,[a("TORCH is built as a Maven multi-module reactor. This split exists primarily to make consent handling swappable (see "),t("a",{href:"./consent.html#_9-extending-consent-handling-consentevaluator"},"Extending Consent Handling"),a(") without forking or rebuilding the core application — the module boundary is what makes "),t("code",null,"torch-consent-mii"),a(" a genuinely separate, replaceable deployment artifact rather than just an internal package.")],-1)),(o(),l(p,null,{default:n(()=>[r(i,{id:"mermaid-6",class:"mermaid",graph:"flowchart%20TD%0A%20%20%20%20API%5B%22torch-consent-api%3Cbr%2F%3E(ConsentEvaluator%20SPI)%22%5D%0A%20%20%20%20MII%5B%22torch-consent-mii%3Cbr%2F%3E(default%20implementation)%22%5D%0A%20%20%20%20APP%5B%22torch-app%3Cbr%2F%3E(core%20application)%22%5D%0A%20%20%20%20MII%20--%3E%7Ccompile%7C%20API%0A%20%20%20%20APP%20--%3E%7Ccompile%7C%20API%0A%20%20%20%20APP%20-.-%3E%7Ctest%20only%7C%20MII%0A"})]),fallback:n(()=>[...e[0]||(e[0]=[a(" Loading... ",-1)])]),_:1})),e[3]||(e[3]=h(`<h2 id="modules" tabindex="-1">Modules <a class="header-anchor" href="#modules" aria-label="Permalink to &quot;Modules&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Module</th><th>Packaging</th><th>Depends on</th><th>Purpose</th></tr></thead><tbody><tr><td><code>torch-parent</code></td><td><code>pom</code> (root aggregator)</td><td>—</td><td>Declares the module list, shared dependency versions (<code>dependencyManagement</code>), and shared plugin configuration (<code>pluginManagement</code>). Not itself compiled or tested.</td></tr><tr><td><code>torch-consent-api</code></td><td><code>jar</code></td><td>—</td><td>The <code>ConsentEvaluator</code> extension point: <code>ConsentEvaluator</code>, <code>ConsentContext</code>, <code>PatientSet</code>, <code>ConsentDataClient</code>, plus consent-period value types (<code>Period</code>, <code>NonContinuousPeriod</code>). No MII-specific logic.</td></tr><tr><td><code>torch-consent-mii</code></td><td><code>jar</code></td><td><code>torch-consent-api</code></td><td>The default <code>ConsentEvaluator</code> implementation — the MII Broad Consent pipeline documented in <a href="./consent.html">Consent Handling</a>. Registered via Spring Boot&#39;s auto-configuration SPI (<code>META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</code>), not <code>@Component</code> scanning, so implementations outside TORCH&#39;s own packages are discovered the same way. Packaged as its own jar and shipped separately from the main application jar.</td></tr><tr><td><code>torch-app</code></td><td><code>jar</code> (Spring Boot fat jar)</td><td><code>torch-consent-api</code> (compile), <code>torch-consent-mii</code> (<strong>test only</strong>)</td><td>The core application: REST API, job scheduling, extraction pipeline, cohort query. Depends on <code>torch-consent-mii</code> only so its own integration tests have a real <code>ConsentEvaluator</code> bean available — the production image does not bundle <code>torch-consent-mii</code> inside <code>torch.jar</code>; it is copied in as a separate file (see below).</td></tr></tbody></table><p><code>torch-app</code>&#39;s dependency on <code>torch-consent-mii</code> being test-scoped is the detail that keeps the default swappable: if it were a compile/runtime dependency, <code>torch-consent-mii</code> would be baked into the fat jar and the plugin mechanism below would have nothing to swap.</p><h2 id="how-the-swap-works-at-runtime" tabindex="-1">How the swap works at runtime <a class="header-anchor" href="#how-the-swap-works-at-runtime" aria-label="Permalink to &quot;How the swap works at runtime&quot;">​</a></h2><p><code>torch-app</code>&#39;s Docker image copies <code>torch-consent-mii</code>&#39;s jar into <code>/app/plugins/</code> as a separate file rather than including it in <code>torch.jar</code>. The application is launched via Spring Boot&#39;s <code>PropertiesLauncher</code> with <code>-Dloader.path=$LOADER_PATH</code> (<code>docker-entrypoint.sh</code>, <code>LOADER_PATH</code> default <code>/app/plugins</code>), which adds every jar in that directory to the classpath at startup, on top of what&#39;s already bundled in <code>torch.jar</code>. Replacing the jar in that directory and restarting the container is enough to swap the active <code>ConsentEvaluator</code> — no TORCH rebuild required. Full walkthrough, including how to implement a replacement: <a href="./consent.html#_9-extending-consent-handling-consentevaluator">Extending Consent Handling</a>.</p><h2 id="building-and-testing-per-module" tabindex="-1">Building and testing per module <a class="header-anchor" href="#building-and-testing-per-module" aria-label="Permalink to &quot;Building and testing per module&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Full reactor build / test (from the repo root)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -P</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> download-ontology</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -B</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> package</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DskipTests</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -P</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> download-ontology</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -B</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> verify</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># A single module and the modules it depends on</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -pl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> torch-consent-api</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -am</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compile</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -pl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> torch-consent-mii</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -am</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># torch-app specifically (needs torch-consent-api/torch-consent-mii installed or built alongside)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -pl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> torch-app</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -am</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span></code></pre></div><p>The <code>download-ontology</code> profile (fetches <code>ontology/</code> content used by <code>torch-app</code> at runtime and in its tests) is declared on <code>torch-app</code>&#39;s POM; activating it with <code>-P download-ontology</code> from the reactor root still applies it correctly when building <code>torch-app</code>, since Maven profile activation is a reactor-wide flag, not per-module.</p><p><code>torch-consent-api</code> and <code>torch-consent-mii</code> have no such profile — they carry no MII-runtime-data dependency beyond <code>torch-consent-mii</code>&#39;s bundled <code>consent-code-config.json</code> classpath resource.</p>`,9))])}const w=s(u,[["render",k]]);export{f as __pageData,w as default};

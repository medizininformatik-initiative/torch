import{_ as t,o as i,c as r,a2 as a}from"./chunks/framework.DhsbzC9a.js";const h=JSON.parse('{"title":"Data Extraction","description":"","frontmatter":{},"headers":[],"relativePath":"implementation/data-extraction.md","filePath":"implementation/data-extraction.md","lastUpdated":1784552074000}'),n={name:"implementation/data-extraction.md"};function o(s,e,l,c,d,u){return i(),r("div",null,[...e[0]||(e[0]=[a('<h1 id="data-extraction" tabindex="-1">Data Extraction <a class="header-anchor" href="#data-extraction" aria-label="Permalink to &quot;Data Extraction&quot;">​</a></h1><p>TORCH relies on FHIR StructureDefinitions to interpret resource structure and ensure profile conformance during extraction and reconstruction. All required StructureDefinitions (including base definitions and referenced profiles) are loaded from a configured directory at startup. Custom profiles can be supported by adding their StructureDefinitions to this directory before startup, but changes are not applied dynamically at runtime.</p><p>All referenced StructureDefinitions must be available at startup.</p><p>After resources are loaded they are assigned to the attribute groups that define how they should be extracted.</p><p>The extraction process consists of two main steps:</p><ol><li><strong>Attribute Extraction</strong>: Extract only the attributes specified in the profiles.</li><li><strong>Redaction</strong>: Remove unnecessary data and ensure that the resulting resources remain compliant with their profiles.<br> See <a href="./redaction.html">Redaction</a> for details.</li></ol><h2 id="attribute-extraction" tabindex="-1">Attribute Extraction <a class="header-anchor" href="#attribute-extraction" aria-label="Permalink to &quot;Attribute Extraction&quot;">​</a></h2><p>Attribute extraction is driven by the attribute groups defined in the CRTDL and the referenced profiles. The attributes provide the FHIRPath expressions, which are evaluated against the source resources conforming to the referenced profiles to determine which elements should be retained.</p><p>The extraction process is as follows:</p><ol><li><p><strong>Merge extraction rules</strong><br> If multiple attribute groups select the same resource, the union of all extraction rules is applied. This applies when a resource fulfills multiple attribute groups, for example because it conforms to multiple profiles or because it was loaded through different extraction contexts or reference paths. In such cases, TORCH does not duplicate the resource. Instead, it combines the extraction instructions from all matching attribute groups and extracts the resource once using the merged rule set. Hierarchical extraction is supported: selecting an element via a FHIRPath expression (e.g. Patient.name) implicitly includes all of its child elements (e.g. Patient.name.given, Patient.name.family, etc.).</p></li><li><p><strong>Evaluate FHIRPath expressions</strong><br> The configured FHIRPath expressions are evaluated against the source resources to identify matching elements.</p></li><li><p><strong>Reconstruct resources</strong><br> The selected elements are written to new output resources using HAPI FHIR model operations.</p></li></ol><h2 id="nested-list-handling" tabindex="-1">Nested List Handling <a class="header-anchor" href="#nested-list-handling" aria-label="Permalink to &quot;Nested List Handling&quot;">​</a></h2><p>FHIRPath evaluation operates on matching elements without preserving full list context.<br> TORCH therefore applies a structure-aware reconstruction step to ensure that nested list elements are handled correctly.</p><ul><li>Nested elements are preserved <strong>together with their enclosing list structure</strong> where possible.</li><li>Extraction operates on <strong>element matches</strong>, but reconstruction ensures structurally valid FHIR resources.</li><li>Multiple matching elements from lists are retained without flattening unrelated siblings.</li></ul><p>For example, consider an <code>Observation</code> with a <code>CodeableConcept</code> containing multiple <code>coding</code> entries:</p><ul><li>Each <code>CodeableConcept</code> has a list of <code>coding</code> elements</li><li>Each <code>coding</code> contains fields such as <code>system</code> and <code>code</code></li></ul><p>A FHIRPath expression such as:</p><p><code>Observation.code.coding.code</code></p><p>matches all <code>code</code> elements across all <code>coding</code> entries and returns them as a flat collection.</p><p>During reconstruction, TORCH ensures that:</p><ul><li>Each matching <code>code</code> is placed back into its corresponding <code>coding</code> element</li><li>The <code>coding</code> elements remain grouped within their original <code>CodeableConcept</code></li><li>Unrelated sibling elements are not merged or flattened</li></ul><p>As a result, the output preserves a valid FHIR structure even though the matching step itself is not structure-aware.</p><h3 id="limitations-and-considerations" tabindex="-1">Limitations and Considerations <a class="header-anchor" href="#limitations-and-considerations" aria-label="Permalink to &quot;Limitations and Considerations&quot;">​</a></h3><ul><li><p><strong>List context is not part of FHIRPath semantics</strong><br> FHIRPath expressions operate on matching elements without preserving full positional or relational context within nested lists. As a result, extraction is based on element matches, and some structural intent may not be fully recoverable in complex cases.</p></li><li><p><strong>Ambiguity in deeply nested lists</strong><br> For deeply nested or highly complex list structures, it may not always be possible to fully reconstruct the original semantic relationships between elements.</p></li><li><p><strong>Prefer selecting higher-level elements</strong><br> To ensure predictable results, it is recommended to extract at a higher level (e.g. <code>Patient.name</code>) rather than targeting deeply nested primitives (e.g. <code>Patient.name.given</code>), unless the structure is well understood.</p></li></ul>',23)])])}const m=t(n,[["render",o]]);export{h as __pageData,m as default};

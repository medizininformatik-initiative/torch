# Server configuration

server:
  port: 8080
torch:
  profile:
    dir: src/test/resources/StructureDefinitions
  fhir:
    url: http://localhost:8082/fhir
  flare:
    url: http://localhost:8084
  results:
    dir: bundles/
    persistence: P3YT90M # 90 minutes in ISO 8601 format

logging:
  level:
    org.springframework: ${LOG_LEVEL:info}
    de:
      medizininformatikinitiative:
        torch=DEBUG:



